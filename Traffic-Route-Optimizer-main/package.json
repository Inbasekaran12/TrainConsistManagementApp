{
  "name": "smart-traffic-route-optimizer",
  "version": "1.0.0",
  "description": "A browser-based pathfinding visualization app using A* and Uniform Cost Search.",
  "type": "module",
  "scripts": {
    "test": "node --experimental-vm-modules ./node_modules/jest/bin/jest.js --runInBand",
    "start": "npx http-server src -p 8080",
    "lint": "echo \"No lint configuration yet. Add ESLint if desired.\" && exit 0"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^30.4.1"
  }
}
